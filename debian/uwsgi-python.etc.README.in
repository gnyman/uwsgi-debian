Files with '.ini'/'.xml' extension found in this directory are processed by
@@uwsgi_binary@@ init.d script as uWSGI configuration files.

On system boot for each configuration file new uWSGI daemon instance is started
with additional option:
  * for INI: '--ini /etc/uwsgi/@@uwsgi_binary@@/<configuration file>'
  * for XML: '--xmlconfig /etc/uwsgi/@@uwsgi_binary@@/<configuration file>'

See more detailed information at:
  * /usr/share/doc/@@uwsgi_binary@@/README.Debian.gz
  * /etc/default/@@uwsgi_binary@@
