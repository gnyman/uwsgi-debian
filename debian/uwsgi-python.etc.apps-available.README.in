This is staging directory for uWSGI configuration files.

You should place '.ini'/'.xml'/'.yaml'/'.yml' configuration files here and
create soft links to them in /etc/@@uwsgi_binary@@/apps-enabled directory.

See also /etc/@@uwsgi_binary@@/apps-enabled/README.
