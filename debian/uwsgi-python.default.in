# Defaults for @@uwsgi_binary@@ initscript
# sourced by /etc/init.d/@@uwsgi_binary@@

# Run automatically at system startup?
RUN_AT_STARTUP=1

# At startup VERBOSE value is setted in 'no'. So when user invokes
# @@uwsgi_binary@@ initscript, no output is showed.
# It could be unexpected behaviour, because it is common practice for
# initscript to ignore VERBOSE value.
# Here VERBOSE is overriden to conform such the practice.
VERBOSE=yes

# init.d script starts instance of @@uwsgi_binary@@ daemon for each found
# user-created configuration file.
#
# These options are passed to each instance of @@uwsgi_binary@@, runned by
# init.d script.
#
# Note: options '--socket', '--daemonize' and '--pidfile' are always added
#       automatically by init.d script
#
# Default socket location: /var/run/@@uwsgi_binary@@/<confname>/socket
# Tip: you could override '--socket' in configuration file.
DAEMON_OPTS=" \
  --master \
  --no-orphans \
  --processes 2 \
  --logdate \
  --chmod-socket=660 \
  --uid www-data \
  --gid www-data \
"
